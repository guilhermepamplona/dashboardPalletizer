# ============================================================
# requirements.txt — KR180 Palletizing Cell Dashboard
# Python 3.11
# ============================================================

# --- Web framework & WebSocket ---
fastapi==0.111.0
uvicorn[standard]==0.29.0

# --- Variáveis de ambiente ---
python-dotenv==1.0.1

# --- Comunicação com CLP Siemens S7 via ISO-TSAP ---
# Necessita da lib nativa libsnap7 instalada no SO:
#   Windows: snap7.dll é bundled pelo python-snap7
#   Linux:   sudo apt install libsnap7-1 libsnap7-dev
#   (ou rode o install_snap7.sh incluso no README)
python-snap7==1.3
